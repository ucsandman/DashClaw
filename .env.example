# DashClaw Environment Variables
# Copy this file to .env.local and fill in your values

# Deployment mode
# self_host (default): normal dashboard + API, intended for your machine or your cloud account
# demo: hosted read-only sandbox (no writes; /api/* mostly disabled). Intended for dashclaw.io
DASHCLAW_MODE=self_host
NEXT_PUBLIC_DASHCLAW_MODE=self_host

# Required: PostgreSQL connection string (DashClaw stores data here)
# Local (recommended): docker compose up -d db → postgresql://dashclaw:dashclaw@localhost:5432/dashclaw
# Hosted option: Neon (free tier) https://neon.tech → postgresql://user:pass@ep-xyz.neon.tech/neondb
DATABASE_URL=<YOUR_DATABASE_URL>

# Optional: Force a specific DB driver (auto-detected from DATABASE_URL by default)
# "postgres" = direct TCP (local/self-host), "neon" = Neon serverless
# DASHCLAW_DB_DRIVER=postgres

# Required in production: API key to protect /api/* endpoints
# This key maps to the default organization (org_default).
# Additional orgs/keys can be created via /api/orgs or scripts/create-org.mjs
DASHCLAW_API_KEY=<YOUR_SECRET_API_KEY>

# Optional: Override which org the DASHCLAW_API_KEY maps to (default: org_default)
# Set this if you created a named org and want API key traffic routed there.
# DASHCLAW_API_KEY_ORG=org_default

# Required for sensitive settings encryption (32 chars)
ENCRYPTION_KEY=<random-32-char-string-here>

# Optional: SSRF protection - comma-separated list of trusted webhook domains
# Example: slack.com,discord.com,notion.com
WEBHOOK_ALLOWED_DOMAINS=

# Optional: Restrict CORS to your deployment domain
# ALLOWED_ORIGIN=https://your-app.vercel.app

# Optional: Distributed rate limiting (recommended for multi-instance deployments)
# If not set, DashClaw uses an in-memory per-instance limiter.
# UPSTASH_REDIS_REST_URL=https://...
# UPSTASH_REDIS_REST_TOKEN=...

# Optional: In-app middleware rate limiting (defaults are dev-friendly)
# Window and max are per client IP (best-effort). For multi-instance, configure Upstash above.
# Disable (dev only): DASHCLAW_DISABLE_RATE_LIMIT=true
# Customize:
# DASHCLAW_RATE_LIMIT_WINDOW_MS=60000
# DASHCLAW_RATE_LIMIT_MAX=1000

# Optional: Vercel Web Analytics (privacy-friendly page view counting)
# Enabled automatically on Vercel deployments. To enable on non-Vercel hosts:
# NEXT_PUBLIC_ENABLE_VERCEL_ANALYTICS=true

# Realtime backend selection
# memory (default) = single-instance event bus
# redis = cross-instance pub/sub (set REDIS_URL when enabled)
# REALTIME_BACKEND=memory
# REDIS_URL=redis://localhost:6379
# REALTIME_REPLAY_WINDOW_SECONDS=600
# REALTIME_REPLAY_MAX_EVENTS=1000
# REALTIME_ENFORCE_REDIS=false

# Optional: Change the port (default is 3000)
# PORT=3000

# NextAuth.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=<random-32-char-secret>

# GitHub OAuth (https://github.com/settings/developers)
GITHUB_ID=<your-github-oauth-app-client-id>
GITHUB_SECRET=<your-github-oauth-app-client-secret>

# Google OAuth (https://console.cloud.google.com/apis/credentials)
GOOGLE_ID=<your-google-oauth-client-id>
GOOGLE_SECRET=<your-google-oauth-client-secret>

# OIDC Provider (e.g. Authentik, Keycloak)
OIDC_ISSUER_URL=<your-oidc-issuer-url>
OIDC_CLIENT_ID=<your-oidc-client-id>
OIDC_CLIENT_SECRET=<your-oidc-client-secret>
OIDC_DISPLAY_NAME="Authentik"

# Stripe Billing (https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=<your-stripe-secret-key>
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=<your-stripe-publishable-key>
STRIPE_WEBHOOK_SECRET=<your-stripe-webhook-signing-secret>
STRIPE_PRICE_PRO=<price_xxx>
STRIPE_PRICE_TEAM=<price_xxx>

# Email Alerts (https://resend.com)
RESEND_API_KEY=<your-resend-api-key>
# ALERT_FROM_EMAIL=practicalsystems.io@gmail.com

# Cron Security (generate with: openssl rand -hex 32)
CRON_SECRET=<random-64-char-hex>

# Agent Identity Security
# In production, agent signatures are enforced by default.
# Set to false to allow unsigned agent actions (not recommended).
# ENFORCE_AGENT_SIGNATURES=true

# Agent Enrollment Mode
# Set to true to require agents to be pre-registered before submitting actions.
# When false (default), any API key holder can create new agent_ids automatically.
# DASHCLAW_CLOSED_ENROLLMENT=false

# Proxy Trust
# Set to true if running behind a reverse proxy that sets x-forwarded-for / x-real-ip.
# When false (default), these headers are ignored for rate limiting.
# TRUST_PROXY=false
