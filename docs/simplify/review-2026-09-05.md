# Refactor and performance review, 2026-09-05

Scope: d60463d7..6b5ecc1f, the three simplification rounds and subsequent performance/security changes (133 files). The findings and remaining performance work are included in the 5.33.7 release.

## Confirmed and corrected

- High: Node runGoverned and Python run_governed executed callbacks when guard allowed but fallback action creation returned pending_approval. Reproduced in both SDKs with wait:false. Both responses now participate in the approval gate.
- High: the single-call SDK path passed action metadata through guard validation, which dropped reasoning, session_id, parent_action_id, tokens_in, tokens_out, model and cost_estimate. Richer inputs now use canonical action creation; Python signing and configured guard/HITL behavior also retain that path. Minimal inputs retain single-call recording.
- High: URL credential scanning returned clean for passwords containing comma, semicolon or ampersand, and explicit credentials on single-label hosts. Explicit user:password URLs now cover those cases while existing contact-address false-positive tests remain intact.
- Medium: batched plan details removed the previous 200-deviations-per-plan limit. One busy plan could grow every Approvals poll indefinitely. The batch now uses a bounded lateral read for each selected plan, retaining one database request and tenant scoping.

## Remaining opportunities and boundaries

- Completed: recorded guards auto-derive the same hourly idempotency key as action creation, preserving explicit keys and evaluation-only behavior. New Node and Python tests failed before the change and pass after it. Ledger deduplication does not guarantee exactly-once callbacks.
- Completed: decision replay renders the primary record before optional graph, trace and guard evidence. Requests are cancelled on navigation or refresh, and stale responses are ignored. Component regressions failed before the change; a browser check holds graph delivery until the primary record renders, then verifies the Graph tab. This also exposed and repaired missing demo graph fixtures.
- The action-ceiling cache admits up to 30 seconds of additional actions after a below-90-percent read. That is a deliberate approximation, not strict quota enforcement. Keep it separate from any hard spending limit.
- The simplification log already records pre-existing bootstrap JWT detection and malformed policy-rules parsing defects. They were not introduced by these commits.
- GitNexus was 395 commits stale with incompatible schema; rebuilding restored graph analysis. Full-text search remains unavailable because its native extension cannot load. Repowise reported stale documentation and a degraded semantic embedder; source and executable checks were used as authority.

## Verification

Baseline: 518 test files passed, one skipped; 5,388 tests passed, five skipped. Typecheck, production build and the six contract/artifact/version checks passed. Lint had four existing navigation warnings and no errors.

Release verification: 5,393 Vitest tests passed, five skipped; all 136 Python SDK tests passed. Typecheck, production build, contract/artifact/version checks, guide drift and documentation checks passed; lint retained the same four warnings. The delayed-graph browser regression passed. Adversarial review also exposed a late reissue response loading a previous decision after navigation; its new regression failed before correction and passed afterward.

Adversarial checks: 12 Node/scanner scenarios and six Python scenarios cover stricter fallback approvals, wait:false, preserved metadata, minimal fast-path calls, signing fallback and the missed URL forms. All passed after correction. The existing 15 Python run-governed tests passed. Existing test files were not edited.

The deviation query retains the existing repository test contract. It has not been benchmarked against a production-sized database; reduced response volume follows from the per-plan SQL limit, not a measured production latency claim.

## Deviations

The initially unbounded batch was replaced with a lateral query over tenant-scoped selected plans. The first version used unnest and failed the existing ANY(planIds) assertion; selecting the actual plan rows preserves that contract and avoids repeated IDs. No tests were changed to obtain a pass.

The operator also requested repair of the separate Agnostic harness hook discovered during this review. Its stdout serialization is now repaired in the harness checkout, with eight process-level regression checks passing. The hook remains enabled.

Release preparation built successfully but could not capture live guide examples because local database health returned unhealthy. The existing guide drift contract permits patch-version differences, so this release retains those captures and runs the drift gate directly. No database configuration or schema was changed.
