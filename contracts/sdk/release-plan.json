{
  "node": {
    "current_version": "4.33.0",
    "next_bump": "none",
    "reason": "4.33.0 ships the calibration flywheel automation (miner synthetic filter + proposal mode + scheduled proposal workflow) — no Node SDK source change; the version advances per the unified model but the SDK is NOT republished. npm stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.33.0",
    "next_bump": "none",
    "reason": "4.33.0 ships the calibration flywheel automation (miner synthetic filter + proposal mode + scheduled proposal workflow) — no Python SDK source change; the version advances per the unified model but the SDK is NOT republished. PyPI stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  }
}
