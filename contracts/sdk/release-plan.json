{
  "node": {
    "current_version": "5.0.1",
    "next_bump": "none",
    "reason": "5.0.1 carries a small real Node SDK source change (fe7ef21b: dead-code purge in sdk/dashclaw.js after the v5 cull) - the SDK IS republished to npm at 5.0.1 (credential-gated human tail: npm run release:sdks).",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.0.1",
    "next_bump": "none",
    "reason": "5.0.1 carries a small real Python SDK source change (fe7ef21b: client.py cleanup after the v5 cull) - the SDK IS republished to PyPI at 5.0.1 (credential-gated human tail: npm run release:sdks).",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.0.1 is the first-run sign-in ship: `dashclaw up` prints the admin password (previously swallowed with the piped setup stderr) and opens the browser already signed in via a single-use 15-minute DASHCLAW_LOGIN_OTT consumed by /api/auth/local; a failed Claude Code hooks install (fresh machine, no Python) is now non-fatal to the sign-in tail. @dashclaw/cli republishes at 0.8.2 (0.8.1 shipped the feature, 0.8.2 the non-fatal connect). Both SDKs republish at 5.0.1 for the small fe7ef21b source deltas.",
  "domains": [
    "platform"
  ]
}
