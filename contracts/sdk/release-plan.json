{
  "node": {
    "current_version": "5.27.3",
    "next_bump": "none",
    "reason": "v5.27.3 is platform-only (mkfs grades as a raw device write so the evidence-gated catastrophe line actually reaches its threshold) — no Node/Python SDK source change; version advances per the unified model but the SDKs are NOT republished; registry stays at 5.27.2.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.27.3",
    "next_bump": "none",
    "reason": "v5.27.3 is platform-only (mkfs grades as a raw device write so the evidence-gated catastrophe line actually reaches its threshold) — no Node/Python SDK source change; version advances per the unified model but the SDKs are NOT republished; registry stays at 5.27.2.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.27.3 follows 5.27.2 within the hour: on the live guard mkfs carried the protected_target flag but graded 90 (no device-write modifier) and ran unheld under the flag-gated line. mkfs now rides the raw-device-write branch (+20, flags device_write + protected_target) and totals 100. Zero new API routes, zero new pages. Neither SDK source changed.",
  "domains": [
    "platform"
  ]
}
