{
  "node": {
    "current_version": "4.43.0",
    "next_bump": "none",
    "reason": "4.43.0 is ADR Phase 2 batch 4 (guard-layer hardening): operator-approval grants become single-use + fingerprint-bound (atomic UPDATE consuming approval_grant_used_at from drizzle/0045, bound to action_type — one approval covers exactly one retried evaluation; idempotent replays keep the UX), and the rate_limit runaway valve counts guard_decisions (every evaluation, new org/agent/created_at index) instead of action_records so guard-only integrations trip it — platform only, no Node/Python SDK source change; SDKs NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.43.0",
    "next_bump": "none",
    "reason": "4.43.0 is ADR Phase 2 batch 4 (guard-layer hardening): operator-approval grants become single-use + fingerprint-bound (atomic UPDATE consuming approval_grant_used_at from drizzle/0045, bound to action_type — one approval covers exactly one retried evaluation; idempotent replays keep the UX), and the rate_limit runaway valve counts guard_decisions (every evaluation, new org/agent/created_at index) instead of action_records so guard-only integrations trip it — platform only, no Node/Python SDK source change; SDKs NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  }
}
