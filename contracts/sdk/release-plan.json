{
  "node": {
    "current_version": "4.7.0",
    "next_bump": "none",
    "reason": "4.7.0 is a platform UI feature release (sitewide interactions v2): the v4.6.0 context-menu + multi-select systems extended to every entity-bearing surface, an EntityLink primitive making on-page references clickable, ticker severity deep-links, inline approve/deny from the notification bell (reusing the existing org-scoped POST /api/approvals/{id}), read-only demo fixtures so no page renders empty, and a Policy Coach behavior-recorder reliability fix. No Node SDK surface change (unchanged at 126, byte-identical to 4.6.x); republishes at 4.7.0 per the unified-version model. Self-dep stays ^4.x (within the major).",
    "domains": ["platform"]
  },
  "python": {
    "current_version": "4.7.0",
    "next_bump": "none",
    "reason": "4.7.0 is a platform UI feature release (sitewide interactions v2: context menu + multi-select everywhere, clickable references + ticker, bell approve/deny, demo data backfill, Policy Coach recorder reliability fix). No Python SDK change — Python surface unchanged at 224 (byte-identical to 4.6.x) and republishes at 4.7.0 per the unified-version model.",
    "domains": ["platform"]
  }
}
