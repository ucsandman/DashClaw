{
  "node": {
    "current_version": "5.23.4",
    "next_bump": "none",
    "reason": "5.23.4 is a platform+CLI ship: the dashclaw install openclaw onboarding wizard and its docs/marketing accuracy sweep. sdk/ source did NOT change this release. The unified-version model advances the number anyway. Pushing the v5.23.4 tag fires release.yml, whose publish-npm job only skips versions ALREADY on the registry - decide the publish by deciding whether to tag, not by editing this file.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.23.4",
    "next_bump": "none",
    "reason": "5.23.4 is a platform+CLI ship (install openclaw onboarding wizard). sdk-python/ source did NOT change this release. The unified-version model advances the number anyway. Pushing the v5.23.4 tag fires release.yml, whose publish-pypi job only skips versions ALREADY on the registry - decide the publish by deciding whether to tag, not by editing this file.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.23.4 ships interactive onboarding for dashclaw install openclaw: a bare TTY invocation now offers to create a DashClaw instance (hosted trial signup or an inline dashclaw up on the same machine), collects/mints the API key, and prompts a per-machine agent id (<hostname>-openclaw) - non-TTY runs keep the hard errors. @dashclaw/cli bumps 0.11.0 -> 0.12.0 (independent track; the v5.23.4 tag publishes it). The shared hosted-trial flow moved to cli/lib/trial.js (install claude --trial now uses it too). Zero new routes/SDK methods/MCP tools/policy types. Neither SDK source changed.",
  "domains": [
    "platform"
  ]
}
