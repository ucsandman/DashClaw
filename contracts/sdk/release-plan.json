{
  "node": {
    "current_version": "4.15.0",
    "next_bump": "none",
    "reason": "4.15.0 is platform-only (W3 interruption budget / approval flood guard + bulk resolution, fleet digest + digest tick, signals 16->18, mcp-server lib recompile) — no Node SDK source change; version advances per the unified model but the SDK is NOT republished; npm stays at 4.11.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.15.0",
    "next_bump": "none",
    "reason": "4.15.0 is platform-only (W3 interruption budget / approval flood guard + bulk resolution, fleet digest + digest tick, signals 16->18, mcp-server lib recompile) — no Python SDK source change; version advances per the unified model but the SDK is NOT republished; PyPI stays at 4.11.0.",
    "domains": [
      "platform"
    ]
  }
}
