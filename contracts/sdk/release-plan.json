{
  "node": {
    "current_version": "5.8.1",
    "next_bump": "none",
    "reason": "no Node SDK source change in 5.8.1 - version advances per the unified model but the SDK is NOT republished; the registry stays at 5.6.2 (published 2026-07-28).",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.8.1",
    "next_bump": "none",
    "reason": "no Python SDK source change in 5.8.1 - version advances per the unified model but the SDK is NOT republished; the registry stays at 5.6.2 (published 2026-07-28).",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.8.1 closes audit F3 to the extent software can: the control-plane protected_path policy is converted live to block + ungrantable (verified: writes to settings.json and dashclaw_pretool.py both block), and the docs/marketing honesty pass lands the privilege boundary everywhere the product speaks - the hook seam is a seatbelt against accidents, not a cage against intent; real isolation is a deployment property (container, separate OS user, read-only hook path). Docs + landing copy only; no SDK surface change.",
  "domains": [
    "platform"
  ]
}
