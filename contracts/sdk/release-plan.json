{
  "node": {
    "current_version": "4.20.2",
    "next_bump": "none",
    "reason": "4.20.2 is a platform-only security/reliability patch (halt vs idempotency-replay seam, security-review landmines, context-menu silent-failure, vercel preview auto-migrate, interpreter classifier, HITL re-eval): NO Node SDK source change. Version advances per the unified model but the SDK is NOT republished at 4.20.2 - npm stays at its last published version. The pre-existing 4.20.x republish (idempotency auto-derive) remains owed separately.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.20.2",
    "next_bump": "none",
    "reason": "4.20.2 is a platform-only security/reliability patch (halt vs idempotency-replay seam, security-review landmines, context-menu silent-failure, vercel preview auto-migrate, interpreter classifier, HITL re-eval): NO Python SDK source change. Version advances per the unified model but the SDK is NOT republished at 4.20.2 - PyPI stays at its last published version. The pre-existing 4.20.x republish remains owed separately.",
    "domains": [
      "platform"
    ]
  }
}
