{
  "node": {
    "current_version": "5.17.2",
    "next_bump": "none",
    "reason": "no Node SDK source change in 5.17.2 (UI-only ship: /policies suppressed-patterns reveal, /assumptions Validate control) - version advances per the unified model; the tag-triggered release workflow (G7) republishes the unchanged content at 5.17.2 so the registry stays aligned with the platform version.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.17.2",
    "next_bump": "none",
    "reason": "no Python SDK source change in 5.17.2 (UI-only ship: /policies suppressed-patterns reveal, /assumptions Validate control) - version advances per the unified model; the tag-triggered release workflow (G7) republishes the unchanged content at 5.17.2 so the registry stays aligned with the platform version.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.17.2 two operator-control fixes: the /policies inert-rule banner's \"Review suppressed patterns\" now actually reveals the suppressing grant (it was a dead #suppressed anchor), and /assumptions gains a visible Validate button per row plus bulk Validate (the positive verdict existed only in the right-click menu). Platform/UI only, no Node/Python SDK surface change.",
  "domains": [
    "platform"
  ]
}
