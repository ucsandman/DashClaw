{
  "node": {
    "current_version": "5.9.1",
    "next_bump": "none",
    "reason": "no Node SDK source change in 5.9.1 - version advances per the unified model but the SDK is NOT republished; the registry stays at 5.6.2 (published 2026-07-28).",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.9.1",
    "next_bump": "none",
    "reason": "no Python SDK source change in 5.9.1 - version advances per the unified model but the SDK is NOT republished; the registry stays at 5.6.2 (published 2026-07-28).",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.9.1 closes the Windows .bat/.cmd script-then-execute gap in the hooks and adds the OpenClaw embedded-codex notify bridge (dashclaw codex notify --agent-id + kebab-case payload support). Hooks + CLI only; no SDK surface change.",
  "domains": [
    "platform"
  ]
}
