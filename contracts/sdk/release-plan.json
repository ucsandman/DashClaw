{
  "node": {
    "current_version": "5.24.2",
    "next_bump": "none",
    "reason": "5.24.2 is a docs-only ship from the launch preflight sweep: .env.example documents 11 previously code-only operational env names, and the platform guide's setup inventory drops the retired STRIPE_PRICE_PRO/BUSINESS names for STRIPE_PRICE_INDIE/TEAM. sdk/ source did NOT change this release. The unified-version model advances the number anyway. Pushing the v5.24.2 tag fires release.yml, whose publish-npm job only skips versions ALREADY on the registry - decide the publish by deciding whether to tag, not by editing this file.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.24.2",
    "next_bump": "none",
    "reason": "5.24.2 is a docs-only ship (env-name documentation + platform-guide Stripe env-name fix). sdk-python/ source did NOT change this release. The unified-version model advances the number anyway. Pushing the v5.24.2 tag fires release.yml, whose publish-pypi job only skips versions ALREADY on the registry - decide the publish by deciding whether to tag, not by editing this file.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.24.2 is the launch-preflight docs-accuracy patch: .env.example gains 11 operational env names verified against their consumption sites (REALTIME_REDIS_URL alias, DASHCLAW_WITNESS_WINDOW_MINUTES, six STARTUP_SMOKE_* knobs, two LIVE_CANARY_* origins, HOSTED_ADMIN_API_KEY), and the platform guide + coverage provenance replace the retired STRIPE_PRICE_PRO/BUSINESS names with STRIPE_PRICE_INDIE/TEAM. Zero new routes/SDK methods/MCP tools/policy types. Neither SDK source changed.",
  "domains": [
    "platform"
  ]
}
