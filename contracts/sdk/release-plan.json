{
  "node": {
    "current_version": "4.73.0",
    "next_bump": "none",
    "reason": "4.73.0 ships real Node SDK source changes (sdk/dashclaw.js + sdk/legacy/dashclaw-v1.js): runGoverned({wait:false}) no longer silently executes work whose approval is still pending - it throws the new ApprovalPendingError. REPUBLISH: npm moves from 4.63.2 to 4.73.0 via npm run release:sdks.",
    "domains": [
      "platform",
      "node-sdk"
    ]
  },
  "python": {
    "current_version": "4.73.0",
    "next_bump": "none",
    "reason": "4.73.0 ships real Python SDK source changes (dashclaw/client.py + __init__.py): same wait=False approval-bypass fix via ApprovalPendingError. REPUBLISH: PyPI moves from 4.63.2 to 4.73.0 via npm run release:sdks.",
    "domains": [
      "platform",
      "python-sdk"
    ]
  },
  "reason": "v4.73.0 combines the guard hot-path performance pass (hook-call p50 221->124ms on a 10ms-RTT DB; drizzle/0058 idempotency_key column+index replacing the volume-linear jsonb replay scan; Server-Timing header; scripts/bench-guard-hotpath.mjs regression harness) with the silent-failure hardening sweep (SDK wait:false approval bypass fixed in both SDKs, GUARD_AUDIT_PERSIST_FAILED honest 503, MCP audit_error truthfulness, hook enforcement_mode attribution). Both SDKs republish this release.",
  "domains": [
    "platform",
    "node-sdk",
    "python-sdk"
  ]
}
