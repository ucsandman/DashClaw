{
  "node": {
    "current_version": "4.63.2",
    "next_bump": "none",
    "reason": "4.63.2 first-run reliability release: no Node SDK source change, but publishing dashclaw@4.63.2 to npm is REQUIRED anyway - `npx dashclaw up` resolves which app tarball to install from the npm `dashclaw` version, so the local-install fixes (setup hangs, missing drizzle chain) only reach fresh users once 4.63.2 is the npm latest.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.63.2",
    "next_bump": "none",
    "reason": "4.63.2 first-run reliability release: no Python SDK source change; republish rides along with the Node publish per the unified-version model (release:sdks is idempotent).",
    "domains": [
      "platform"
    ]
  },
  "reason": "v4.63.2 fixes four first-run killers in `npx dashclaw up` on local databases (idle-connection hang, missing drizzle chain in local setup, invalid token_budgets DDL, dotenv import crash, stdout pipe deadlock) plus @dashclaw/cli 0.6.2 port-conflict handling. npm publish required so `up` fetches the fixed tarball.",
  "domains": [
    "platform"
  ]
}
