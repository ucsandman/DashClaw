{
  "node": {
    "current_version": "5.27.8",
    "next_bump": "none",
    "reason": "v5.27.8 is platform-only (sidebar Tuning link scroll fix; calm-200 claim probe quieted off-hosted) — no Node/Python SDK source change. The unified version advances and release.yml's tag/dispatch publish mirrors it to the registries (skip-if-published guards apply); package contents are identical to 5.27.4.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.27.8",
    "next_bump": "none",
    "reason": "v5.27.8 is platform-only (sidebar Tuning link scroll fix; calm-200 claim probe quieted off-hosted) — no Node/Python SDK source change. The unified version advances and release.yml's tag/dispatch publish mirrors it to the registries (skip-if-published guards apply); package contents are identical to 5.27.4.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.27.8 fixes two UI/console annoyances: the sidebar Tuning link (/policies#calibration) did nothing when the hash was already in the URL — the nav click handler now scrolls the calibration section into view itself — and the calm-200 claim probe no longer spams the console on self-host (landed unreleased in 0d2dfb0f). Zero new routes, zero new pages. Neither SDK source changed.",
  "domains": [
    "platform"
  ]
}
