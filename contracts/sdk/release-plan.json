{
  "node": {
    "current_version": "4.42.0",
    "next_bump": "none",
    "reason": "4.42.0 is ADR Phase 2 batch 3 (D2 one-knob outage contract): fast evaluation failures (policy load / risk read / any pre-deadline phase throw) now degrade via resolveDegradedAction() with _degraded.kind:error and persist through the mandatory audit gate instead of rejecting as 5xx — DASHCLAW_GUARD_FALLBACK covers slow AND fast failures; persistence failures still throw (an unaudited decision is never returned) — platform only, no Node/Python SDK source change; SDKs NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.42.0",
    "next_bump": "none",
    "reason": "4.42.0 is ADR Phase 2 batch 3 (D2 one-knob outage contract): fast evaluation failures (policy load / risk read / any pre-deadline phase throw) now degrade via resolveDegradedAction() with _degraded.kind:error and persist through the mandatory audit gate instead of rejecting as 5xx — DASHCLAW_GUARD_FALLBACK covers slow AND fast failures; persistence failures still throw (an unaudited decision is never returned) — platform only, no Node/Python SDK source change; SDKs NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  }
}
