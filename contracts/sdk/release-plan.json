{
  "node": {
    "current_version": "5.23.2",
    "next_bump": "none",
    "reason": "5.23.2 is a platform-only hardening release from a whole-codebase adversarial review (guard fail_closed posture on unreadable provider config, halt-cache generation guard, atomic cron-signals claim, hot-path perf, webhook_deliveries retention, bounded Redis connect #222). sdk/ source did NOT change this release. The unified-version model advances the number anyway. Pushing the v5.23.2 tag fires release.yml, whose publish-npm job only skips versions ALREADY on the registry - decide the publish by deciding whether to tag, not by editing this file.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.23.2",
    "next_bump": "none",
    "reason": "5.23.2 is a platform-only hardening release from a whole-codebase adversarial review. sdk-python/ source did NOT change this release. The unified-version model advances the number anyway. Pushing the v5.23.2 tag fires release.yml, whose publish-pypi job only skips versions ALREADY on the registry - decide the publish by deciding whether to tag, not by editing this file.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.23.2 is a hardening release from a whole-codebase adversarial review (10 confirmed findings, all fixed with regression tests): external-verdict fail_closed posture now applies to an unreadable saved provider config (config_unreadable) instead of silently skipping; a per-org generation counter closes the halt-cache stale-refill race; cron/signals claims new signals atomically so overlapping runs cannot double-notify; guard ?record=true returns the DLP security summary and recovers 23505 idempotency races; webhook_check policies evaluate concurrently; migration 0074 indexes the predictive-risk history query; webhook_deliveries gets 30-day retention; the Redis rate-limit connect is explicitly bounded (#222, parallel session). Zero new routes/SDK methods/MCP tools/policy types. Neither SDK source changed; the CLI did not change.",
  "domains": [
    "platform"
  ]
}
