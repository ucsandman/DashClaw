{
  "node": {
    "current_version": "5.12.0",
    "next_bump": "none",
    "reason": "no Node SDK source change in 5.12.0 - version advances per the unified model; the tag-triggered release workflow (G7) republished the unchanged content at 5.12.0 so npm stays aligned with the platform version.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.12.0",
    "next_bump": "none",
    "reason": "no Python SDK source change in 5.12.0 - version advances per the unified model; the tag-triggered release workflow (G7) republished the unchanged content at 5.12.0 so PyPI stays aligned with the platform version.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.12.0 hosted paid tier measurement layer (G4+G5): per-org monthly usage_rollups table + GET /api/usage + /usage dashboard page, and org-keyed Redis-backed rate limits on the guard and record routes. Platform only, no Node/Python SDK surface change.",
  "domains": [
    "platform"
  ]
}
