{
  "node": {
    "current_version": "5.16.0",
    "next_bump": "none",
    "reason": "no Node SDK source change in 5.16.0 - version advances per the unified model; the tag-triggered release workflow (G7) republishes the unchanged content at 5.16.0 so the registry stays aligned with the platform version.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.16.0",
    "next_bump": "none",
    "reason": "no Python SDK source change in 5.16.0 - version advances per the unified model; the tag-triggered release workflow (G7) republishes the unchanged content at 5.16.0 so the registry stays aligned with the platform version.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.16.0 hosted entitlement enforcement (seat caps + monthly action ceilings at existing capacity seams), the public /pricing page, and checkout/portal self-heal on stale Stripe customer ids. Platform only, no Node/Python SDK surface change.",
  "domains": [
    "platform"
  ]
}
