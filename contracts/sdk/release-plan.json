{
  "node": {
    "current_version": "5.0.0",
    "next_bump": "major",
    "reason": "v5.0.0 collapses the Node SDK to the governance core (149 -> 28 methods) and deletes the deprecated dashclaw/legacy subpath. SDK source changed substantially - the SDK IS republished to npm at 5.0.0 (credential-gated human tail: npm run release:sdks).",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.0.0",
    "next_bump": "major",
    "reason": "v5.0.0 collapses the Python SDK to the governance core (234 -> 51 methods) and removes the dashclaw[langchain] extra. SDK source changed substantially - the SDK IS republished to PyPI at 5.0.0 (credential-gated human tail: npm run release:sdks).",
    "domains": [
      "platform"
    ]
  },
  "reason": "v5.0.0 is the cull (THESIS.md): DashClaw collapses to the governance loop (intercept -> decide -> approve -> prove). Both SDKs' source changed materially and republish at 5.0.0; @dashclaw/cli republishes at 0.8.0 (8 commands removed); the dashclaw plugin bundle advances to 3.0.0 (breaking hook changes). See docs/releases/2026-07-07-v5-kill-ledger.md.",
  "domains": [
    "platform"
  ]
}
