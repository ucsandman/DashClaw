{
  "node": {
    "current_version": "4.21.0",
    "next_bump": "none",
    "reason": "4.21.0 ships the npx dashclaw up one-command local installer. SDK source CHANGED: the dashclaw package gained a bin shim (sdk/bin/dashclaw-shim.js + bin field) so `npx dashclaw <args>` forwards to @dashclaw/cli. Republish OWED at 4.21.0 (this also clears the carried-over 4.20.x republish — idempotency auto-derive). @dashclaw/cli bumps independently to 0.5.0 and must be published too for `npx dashclaw up` to work.",
    "domains": [
      "platform",
      "sdk-node",
      "cli"
    ]
  },
  "python": {
    "current_version": "4.21.0",
    "next_bump": "none",
    "reason": "4.21.0 unified bump. No Python SDK source change in the up installer (it is a Node CLI + Node SDK bin shim), but the unified version advances; republish the Python SDK at 4.21.0 alongside Node so the registries stay aligned with the carried-over 4.20.x republish.",
    "domains": [
      "platform"
    ]
  }
}
