{
  "node": {
    "current_version": "5.27.2",
    "next_bump": "none",
    "reason": "v5.27.2 is platform-only (mass-destructive line keys on protected_target evidence, not the risk score) — no Node/Python SDK source change; version advances per the unified model but the SDKs are NOT republished; registry stays at 5.27.1.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.27.2",
    "next_bump": "none",
    "reason": "v5.27.2 is platform-only (mass-destructive line keys on protected_target evidence, not the risk score) — no Node/Python SDK source change; version advances per the unified model but the SDKs are NOT republished; registry stays at 5.27.1.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.27.2 adds rules.only_evidence_flags to risk_threshold and pins the catastrophe-only and claude-code-starter mass-destructive lines to the classifier's protected_target flag (rm/find on root/home/system/drive, raw device write, mkfs). The bare threshold-100 line held `cat .env.example` and heredoc prose containing `dd ` — five hand-approvals in one morning, none a catastrophe. Seeded rows are upgraded in place on every deploy. Zero new API routes, zero new pages. Neither SDK source changed.",
  "domains": [
    "platform"
  ]
}
