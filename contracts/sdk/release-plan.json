{
  "node": {
    "current_version": "5.27.1",
    "next_bump": "none",
    "reason": "v5.27.1 is platform-only (default packs hold instead of block at risk 100; seeded rows migrated in place) — no Node/Python SDK source change; version advances per the unified model but the SDKs are NOT republished; registry stays at 5.26.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.27.1",
    "next_bump": "none",
    "reason": "v5.27.1 is platform-only (default packs hold instead of block at risk 100; seeded rows migrated in place) — no Node/Python SDK source change; version advances per the unified model but the SDKs are NOT republished; registry stays at 5.26.0.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.27.1 flips the catastrophe-only and claude-code-starter risk-100 lines from block to require_approval (an irreversible deploy scores exactly 100 and was being refused with no approval card), renames them \"Hold Mass-Destructive Operations for Approval\", and migrates already-seeded rows in place on every deploy. Zero new API routes, zero new pages. Neither SDK source changed.",
  "domains": [
    "platform"
  ]
}
