{
  "node": {
    "current_version": "4.41.0",
    "next_bump": "none",
    "reason": "4.41.0 is ADR Phase 2 batch 2 (verified-identity gate, D1): evaluateAccess never grants an unverified agent_id assertion a more permissive outcome than the org default (per-agent allow-lists require JWKS-verified identity; identity_downgrade in the result), the capability invoke route joins the shared resolveAgentIdentity contract (JWT sub override, verification_status in guard context, real verified flags on action records), access/check gains a ?verified preview param, Access tab explains the semantic — platform only, no Node/Python SDK source change; SDKs NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.41.0",
    "next_bump": "none",
    "reason": "4.41.0 is ADR Phase 2 batch 2 (verified-identity gate, D1): evaluateAccess never grants an unverified agent_id assertion a more permissive outcome than the org default (per-agent allow-lists require JWKS-verified identity; identity_downgrade in the result), the capability invoke route joins the shared resolveAgentIdentity contract (JWT sub override, verification_status in guard context, real verified flags on action records), access/check gains a ?verified preview param, Access tab explains the semantic — platform only, no Node/Python SDK source change; SDKs NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  }
}
