{
  "node": {
    "current_version": "4.7.5",
    "next_bump": "none",
    "reason": "4.7.5 is a platform hardening ship (supergoal sweep phases 3-5): P3 surfaced silent fetch failures across 14 pages/components + a stale-data fix; P4 batched the per-signal and learning N+1 INSERT loops into chunked multi-row INSERTs and capped unbounded SELECTs (new signals.repository); P5 wired the previously-ignored eval-stats agent_id/scorer_name filters, added ::timestamptz casts to TEXT created_at comparisons, and coerced numeric-as-string aggregates. No Node SDK source change (surface unchanged at 126): the version advances per the unified model but the SDKs are NOT republished — npm stays at 4.7.2 (last SDK release). Self-dep stays ^4.x (within the major).",
    "domains": ["platform"]
  },
  "python": {
    "current_version": "4.7.5",
    "next_bump": "none",
    "reason": "4.7.5 is a platform hardening ship (supergoal sweep phases 3-5): silent-failure/stale-data UX sweep (P3), query-performance batching + LIMIT caps (P4), and repository correctness — eval-stats filter wiring, ::timestamptz casts, numeric coercion (P5). No Python SDK source change (surface unchanged at 224): the version advances per the unified model but the SDKs are NOT republished — PyPI stays at 4.7.2 (last SDK release).",
    "domains": ["platform"]
  }
}
