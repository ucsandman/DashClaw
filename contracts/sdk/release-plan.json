{
  "node": {
    "current_version": "5.0.2",
    "next_bump": "none",
    "reason": "5.0.2 is platform-only (workspace-import idempotency fix) - no Node SDK source change; the version advances per the unified model but the SDK is NOT republished; npm stays at the last SDK release.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.0.2",
    "next_bump": "none",
    "reason": "5.0.2 is platform-only (workspace-import idempotency fix) - no Python SDK source change; the version advances per the unified model but the SDK is NOT republished; PyPI stays at the last SDK release.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.0.2 fixes the workspace-import 23505: a bundle guard policy arriving under a foreign id but a name the target org already has (the trial default pack case) now skips via an org-scoped (org_id, name) NOT EXISTS alongside ON CONFLICT (id). Found live by drill:hosted the night it shipped v5.0.1.",
  "domains": [
    "platform"
  ]
}
