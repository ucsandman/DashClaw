{
  "node": {
    "current_version": "4.45.0",
    "next_bump": "none",
    "reason": "4.45.0 completes roadmap v3.3 (fresh-install truth) and the ADR Phase 2 queue: cross-org isolation smoke suite (scripts/cross-org-smoke.mjs, 31 checks, two seeded orgs probing 9 governance families over live HTTP), startup-smoke CI now gates on it plus the doctor write-canary against fresh Postgres, and the no-silent-catch guard extends to app/api/** + repositories with the line-level best-effort: pragma. Platform only — no Node/Python SDK source change; SDKs NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.45.0",
    "next_bump": "none",
    "reason": "4.45.0 completes roadmap v3.3 (fresh-install truth) and the ADR Phase 2 queue: cross-org isolation smoke suite (scripts/cross-org-smoke.mjs, 31 checks, two seeded orgs probing 9 governance families over live HTTP), startup-smoke CI now gates on it plus the doctor write-canary against fresh Postgres, and the no-silent-catch guard extends to app/api/** + repositories with the line-level best-effort: pragma. Platform only — no Node/Python SDK source change; SDKs NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  }
}
