{
  "node": {
    "current_version": "5.11.3",
    "next_bump": "none",
    "reason": "no Node SDK source change in 5.11.3 - version advances per the unified model but the SDK is NOT republished; the registry stays at 5.6.2 (published 2026-07-28).",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.11.3",
    "next_bump": "none",
    "reason": "no Python SDK source change in 5.11.3 - version advances per the unified model but the SDK is NOT republished; the registry stays at 5.6.2 (published 2026-07-28).",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.11.3 makes webhook health telemetry honest on every delivery path: approval-event deliveries and the test button now update last_triggered_at and failure_count (previously only the signals-cron path did), so approval-only webhooks heal, auto-disable at 10 failures, and stop showing 'Last triggered: Never' after successful deliveries. Platform only; no Node/Python SDK surface change.",
  "domains": [
    "platform"
  ]
}
