{
  "node": {
    "current_version": "5.33.7",
    "next_bump": "none",
    "reason": "Both SDKs changed: preserve fallback approvals and action metadata, and auto-derive recorded guard retry keys. Republish Node and Python SDKs with this release.",
    "domains": [
      "platform",
      "actions",
      "guard"
    ]
  },
  "python": {
    "current_version": "5.33.7",
    "next_bump": "none",
    "reason": "Both SDKs changed: preserve fallback approvals and action metadata, and auto-derive recorded guard retry keys. Republish Node and Python SDKs with this release.",
    "domains": [
      "platform",
      "actions",
      "guard"
    ]
  },
  "reason": "Both SDKs changed: preserve fallback approvals and action metadata, and auto-derive recorded guard retry keys. Republish Node and Python SDKs with this release.",
  "domains": [
    "platform",
    "actions",
    "guard"
  ]
}
