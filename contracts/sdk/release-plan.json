{
  "node": {
    "current_version": "4.50.0",
    "next_bump": "none",
    "reason": "4.49.1 ships roadmap v4.1 (own-fleet interruption noise): the shared synthetic-traffic predicate widens to the guard-load harness (loadtest-* agents) and the loadtest.*/liveproof.* action-type families, so flood counting, posture, tightening proposals, and calibration mining stop reporting harness traffic as real interrupts (the 2026-07-04 '1,802 interrupts' flood banner was the load harness). No Node/Python SDK source change \u2014 version advances per the unified model but the SDKs are NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.50.0",
    "next_bump": "none",
    "reason": "4.49.1 ships roadmap v4.1 (own-fleet interruption noise): the shared synthetic-traffic predicate widens to the guard-load harness (loadtest-* agents) and the loadtest.*/liveproof.* action-type families, so flood counting, posture, tightening proposals, and calibration mining stop reporting harness traffic as real interrupts (the 2026-07-04 '1,802 interrupts' flood banner was the load harness). No Node/Python SDK source change \u2014 version advances per the unified model but the SDKs are NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  },
  "reason": "v4.50.0 coverage truth (roadmap v4.2): no Node/Python SDK source change - version advances per the unified model but the SDKs are NOT republished; registry stays at 4.32.0.",
  "domains": [
    "platform"
  ]
}
