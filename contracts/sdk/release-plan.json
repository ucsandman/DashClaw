{
  "node": {
    "current_version": "4.13.0",
    "next_bump": "none",
    "reason": "4.13.0 is platform-only (token attribution-coverage metric + /spend warning, close-the-loop docs) — no Node SDK source change; version advances per the unified model but the SDK is NOT republished; npm stays at 4.11.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.13.0",
    "next_bump": "none",
    "reason": "4.13.0 is platform-only (token attribution-coverage metric + /spend warning, close-the-loop docs) — no Python SDK source change; version advances per the unified model but the SDK is NOT republished; PyPI stays at 4.11.0.",
    "domains": [
      "platform"
    ]
  }
}
