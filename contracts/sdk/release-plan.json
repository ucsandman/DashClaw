{
  "node": {
    "current_version": "5.7.0",
    "next_bump": "none",
    "reason": "no Node SDK source change in 5.7.0 - version advances per the unified model but the SDK is NOT republished; the registry stays at 5.6.2 (published 2026-07-28).",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.7.0",
    "next_bump": "none",
    "reason": "no Python SDK source change in 5.7.0 - version advances per the unified model but the SDK is NOT republished; the registry stays at 5.6.2 (published 2026-07-28).",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.7.0 is the F0 enforcement-visibility release (governance gap audit 2026-08-05): action_records gains enforcement_mode + executed_despite (drizzle/0066), the hooks witness gated actions that execute anyway (observe-mode block/require_approval state -> PostToolUse executed_despite stamp via a status-gated PATCH branch), /approvals and /decisions carry a red observe-mode banner, unenforced ledger rows render distinctly, observe_mode signal is red, a red executed_despite_block signal exists, and gov_observe_mode carries a concrete fix. Platform + hooks only; no SDK surface change.",
  "domains": [
    "platform"
  ]
}
