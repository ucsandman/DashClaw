{
  "node": {
    "current_version": "4.4.5",
    "next_bump": "none",
    "reason": "4.4.5 is a platform-only maintenance patch from a /dashclaw-quality find-and-fix sweep. Fixes `npm run dev` (it 500'd on every route — `next dev` defaulted to Turbopack, which can't apply the webpack `.js`->`.ts` extensionAlias, so middleware.js failed to resolve its demo imports; dev now runs `next dev --webpack -p 3000` to match the build toolchain). Also clears two react-hooks/exhaustive-deps warnings in /capabilities/[capabilityId] and a TS2532 in the recent-digest unit test. No SDK surface change — Node SDK unchanged at 126 (byte-identical to 4.4.4) and republishes at 4.4.5 per the unified-version model. Self-dep stays ^4.2.0 (within the major).",
    "domains": ["platform"]
  },
  "python": {
    "current_version": "4.4.5",
    "next_bump": "none",
    "reason": "4.4.5 is a platform-only maintenance patch from a /dashclaw-quality find-and-fix sweep (fixed `npm run dev` 500-on-every-route by switching dev to `next dev --webpack`, plus two exhaustive-deps warnings and one test TS error). No new SDK surface. Python SDK unchanged at 224 (byte-identical to 4.4.4) and republishes at 4.4.5 per the unified-version model.",
    "domains": ["platform"]
  }
}
