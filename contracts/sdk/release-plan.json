{
  "node": {
    "current_version": "4.74.0",
    "next_bump": "none",
    "reason": "No Node SDK source change in 4.74.0 - the version advances per the unified model but the SDK is NOT republished; npm stays at 4.73.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.74.0",
    "next_bump": "none",
    "reason": "No Python SDK source change in 4.74.0 - the version advances per the unified model but the SDK is NOT republished; PyPI stays at 4.73.0.",
    "domains": [
      "platform"
    ]
  },
  "reason": "v4.74.0 ships the calibrated interruption controller (default off, shadow-mode first): an online adaptive-conformal threshold learned from human approve/deny verdicts holds an operator-set false-interruption rate with a distribution-free bound, per-agent e-process alarms give anytime-valid escalation, active mode is tighten-only (raises to require_approval; never loosens, never touches block), and the /calibration operator page + admin route /api/calibration/controller expose it. Companion doc: docs/architecture/governance-core-theory.md (the mathematical foundation for the governance core). Platform-only: the SDKs are not republished.",
  "domains": [
    "platform"
  ]
}
