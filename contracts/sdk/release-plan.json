{
  "node": {
    "current_version": "4.7.1",
    "next_bump": "none",
    "reason": "4.7.1 is a platform UI patch: the site-wide right-click context menu now covers the ENTIRE site — a generic fallback menu (Copy / Copy page link / Reload) appears over blank space, panels, headings, and untagged content, not only over tagged entity rows, so the native browser menu no longer surfaces there. Text-entry fields keep the native menu (browsers block programmatic Paste). No Node SDK surface change (unchanged at 126); republishes at 4.7.1 per the unified-version model. Self-dep stays ^4.x (within the major).",
    "domains": ["platform"]
  },
  "python": {
    "current_version": "4.7.1",
    "next_bump": "none",
    "reason": "4.7.1 is a platform UI patch (site-wide context-menu fallback so the whole site is right-clickable, native menu no longer surfaces over untagged areas). No Python SDK change — Python surface unchanged at 224 and republishes at 4.7.1 per the unified-version model.",
    "domains": ["platform"]
  }
}
