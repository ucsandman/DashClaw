{
  "node": {
    "current_version": "4.47.0",
    "next_bump": "none",
    "reason": "4.47.0 closes roadmap v3.5 (approval-flood guard) as a closeout audit: the W3 flood guard had already shipped in v4.15.0 and the drafting sweep missed it. This ship fixes the one real gap — flood counting now excludes synthetic traffic (shared v3.1 predicate), so smoke runs can never trip the fleet budget, suppress real per-action pings, or mint the approval_flood signal — and adds the ephemeral ?include_synthetic=1 diagnostic view plus smoke U1-U4 (95 checks). No Node/Python SDK source change — version advances per the unified model but the SDKs are NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.47.0",
    "next_bump": "none",
    "reason": "4.47.0 closes roadmap v3.5 (approval-flood guard) as a closeout audit: the W3 flood guard had already shipped in v4.15.0 and the drafting sweep missed it. This ship fixes the one real gap — flood counting now excludes synthetic traffic (shared v3.1 predicate), so smoke runs can never trip the fleet budget, suppress real per-action pings, or mint the approval_flood signal — and adds the ephemeral ?include_synthetic=1 diagnostic view plus smoke U1-U4 (95 checks). No Node/Python SDK source change — version advances per the unified model but the SDKs are NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  }
}
