{
  "node": {
    "current_version": "4.36.3",
    "next_bump": "none",
    "reason": "4.36.3 fixes the hosted trial mint being demo-blocked: the trial instance (DASHCLAW_HOSTED=true) no longer honors the dashclaw_demo cookie (hosted.dashclaw.io matched the *.dashclaw.io marketing-host check), and the demo passthrough list now runs before the write block so /api/hosted POSTs reach the real route — no Node/Python SDK source change; SDKs NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.36.3",
    "next_bump": "none",
    "reason": "4.36.3 fixes the hosted trial mint being demo-blocked: the trial instance (DASHCLAW_HOSTED=true) no longer honors the dashclaw_demo cookie (hosted.dashclaw.io matched the *.dashclaw.io marketing-host check), and the demo passthrough list now runs before the write block so /api/hosted POSTs reach the real route — no Node/Python SDK source change; SDKs NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  }
}
