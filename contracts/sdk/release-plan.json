{
  "node": {
    "current_version": "4.44.0",
    "next_bump": "none",
    "reason": "4.44.0 ships the doctor write-path canary (roadmap v3.3 core): a write-canary doctor category live-exercises the heartbeat/action-ledger/guard-audit write paths via the real repository writers under an isolated canary org (insert, verify, delete) — a dead write path is a FAIL with the migrate auto-fix, rendered on /setup 'Write-path health' and /doctor. Platform only — no Node/Python SDK source change; SDKs NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.44.0",
    "next_bump": "none",
    "reason": "4.44.0 ships the doctor write-path canary (roadmap v3.3 core): a write-canary doctor category live-exercises the heartbeat/action-ledger/guard-audit write paths via the real repository writers under an isolated canary org (insert, verify, delete) — a dead write path is a FAIL with the migrate auto-fix, rendered on /setup 'Write-path health' and /doctor. Platform only — no Node/Python SDK source change; SDKs NOT republished. Registry stays at 4.32.0.",
    "domains": [
      "platform"
    ]
  }
}
