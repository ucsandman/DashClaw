{
  "node": {
    "current_version": "5.22.0",
    "next_bump": "none",
    "reason": "5.22.0 is Plan Deviation Events (RFC 2026-08-11): plan-vs-actual deviation recording, the deviation_response policy type (guard policy types 16 -> 17, THESIS amendment), and operator resolution surfaces - all platform-side. sdk/ source did NOT change this release; deviations ride existing response payloads so no new SDK methods. The unified-version model advances the number anyway. Pushing the v5.22.0 tag fires release.yml, whose publish-npm job publishes whatever version the manifests carry and only skips versions ALREADY on the registry - decide the publish by deciding whether to tag, not by editing this file.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.22.0",
    "next_bump": "none",
    "reason": "5.22.0 is Plan Deviation Events (RFC 2026-08-11), platform-side only. sdk-python/ source did NOT change this release; deviations ride existing response payloads so no new SDK methods. The unified-version model advances the number anyway. Pushing the v5.22.0 tag fires release.yml, whose publish-pypi job publishes whatever version the manifests carry and only skips versions ALREADY on the registry - decide the publish by deciding whether to tag, not by editing this file.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.22.0 ships Plan Deviation Events (docs/rfcs/2026-08-11-plan-deviation-events.md): the runtime already computed the plan-vs-actual diff in consumePlanStepGrant's no-match branch and threw it away - now it is durable (plan_deviations), classified (six kinds, act_substitution the flagship), policy-visible (deviation_response, tighten-only, no default row - a fresh install changes behavior not at all), and operator-resolvable on /approvals with an Accept-&-amend-plan path that turns acceptance into a recorded amendment. Neither SDK's source changed; the CLI did not change. Guard hot-path cost is one cached EXISTS probe per org:agent per 30s (cold budget 5 -> 6, warm unchanged; measured ~2ms marginal in-plan, ~0 planless).",
  "domains": [
    "platform"
  ]
}
