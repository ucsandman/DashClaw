{
  "node": {
    "current_version": "4.76.0",
    "next_bump": "none",
    "reason": "No Node SDK source change in 4.76.0 - the version advances per the unified model but the SDK is NOT republished; npm stays at 4.73.0.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.76.0",
    "next_bump": "none",
    "reason": "No Python SDK source change in 4.76.0 - the version advances per the unified model but the SDK is NOT republished; PyPI stays at 4.73.0.",
    "domains": [
      "platform"
    ]
  },
  "reason": "v4.76.0 ships entry-path drills (roadmap v8.3): one-command fresh-machine + hosted stranger drills (scripts/drills/) that exercise the distribution path; a hosted drill-mint token (HOSTED_DRILL_TOKEN, timing-safe, force-labeled source=drill, cohort-excluded) as the operator-held Turnstile substitute; and a CLI fix (rootPostgresOptions/createPostgresUser) the Linux root drill caught live. Platform SDKs unchanged; @dashclaw/cli 0.7.5 (source changed) republishes.",
  "domains": [
    "platform"
  ]
}
