{
  "node": {
    "current_version": "5.15.0",
    "next_bump": "none",
    "reason": "no Node SDK source change in 5.15.0 - version advances per the unified model; the tag-triggered release workflow (G7) republishes the unchanged content at 5.15.0 so the registry stays aligned with the platform version.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.15.0",
    "next_bump": "none",
    "reason": "no Python SDK source change in 5.15.0 - version advances per the unified model; the tag-triggered release workflow (G7) republishes the unchanged content at 5.15.0 so the registry stays aligned with the platform version.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.15.0 DashClaw Pulse: the /widget always-on-top ambient posture surface + its read-only GET /api/widget/pulse snapshot route, SSE named heartbeat, and the /approvals discovery button. Platform only, no Node/Python SDK surface change.",
  "domains": [
    "platform"
  ]
}
