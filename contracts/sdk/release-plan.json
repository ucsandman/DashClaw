{
  "node": {
    "current_version": "4.30.0",
    "next_bump": "none",
    "reason": "4.30.0 ships approvals lifecycle hygiene (owner roadmap v2.3) WITH Node SDK source changes: guard() and createAction() now declare approval_wait_seconds (default 300) so pending approvals get a truthful approval_expires_at stamp, and waitForApproval() treats the new server-side 'expired' status as terminal (ApprovalDeniedError with status='expired'). Republish owed — run npm run release:sdks.",
    "domains": [
      "platform",
      "node-sdk"
    ]
  },
  "python": {
    "current_version": "4.30.0",
    "next_bump": "none",
    "reason": "4.30.0 ships approvals lifecycle hygiene (roadmap v2.3) WITH Python SDK source changes: guard()/create_action() declare approval_wait_seconds (default 300) and wait_for_approval() raises ApprovalDeniedError(decision='expired') when the server expires the approval. Republish owed — run npm run release:sdks.",
    "domains": [
      "platform",
      "python-sdk"
    ]
  }
}
