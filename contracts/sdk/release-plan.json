{
  "node": {
    "current_version": "4.66.2",
    "next_bump": "none",
    "reason": "4.66.2 quality-loop harness hardening: no Node SDK source change - the fix lives in scripts/startup-smoke.mjs and the find-and-fix workflow config. Version advances per the unified model but the Node SDK is NOT republished; npm stays at 4.63.2.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "4.66.2",
    "next_bump": "none",
    "reason": "4.66.2 quality-loop harness hardening: no Python SDK source change. Version advances per the unified model but the Python SDK is NOT republished; PyPI stays at 4.63.2.",
    "domains": [
      "platform"
    ]
  },
  "reason": "v4.66.2 hardens the startup-smoke harness (a port-availability preflight now fails loudly when a stale server holds the target port, instead of silently smoking an old build) and makes the dashclaw-find-and-fix workflow launchable (explicit per-agent model overrides; LF line endings pinned via .gitattributes). Dev tooling only; no SDK changes.",
  "domains": [
    "platform"
  ]
}
