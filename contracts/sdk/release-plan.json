{
  "node": {
    "current_version": "5.27.4",
    "next_bump": "none",
    "reason": "v5.27.4 is platform-only (GET /api/guard/decisions now honors the since and action_type filters the dashclaw_decisions_recent MCP tool advertises; unparseable since is a 400) \u2014 no Node/Python SDK source change. The unified version advances and release.yml's tag publish mirrors it to the registries (skip-if-published guards apply); the package contents are identical to 5.27.3.",
    "domains": [
      "platform"
    ]
  },
  "python": {
    "current_version": "5.27.4",
    "next_bump": "none",
    "reason": "v5.27.4 is platform-only (GET /api/guard/decisions now honors the since and action_type filters the dashclaw_decisions_recent MCP tool advertises; unparseable since is a 400) \u2014 no Node/Python SDK source change. The unified version advances and release.yml's tag publish mirrors it to the registries (skip-if-published guards apply); the package contents are identical to 5.27.3.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.27.4 makes the decisions-ledger query contract true: the route silently ignored the since and action_type params the MCP retrospection tool forwards, so 'what have I done recently?' answered with unfiltered history. Both filters now reach SQL; invalid since is a 400. Zero new routes, zero new pages. Neither SDK source changed.",
  "domains": [
    "platform"
  ]
}