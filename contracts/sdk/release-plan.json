{
  "node": {
    "current_version": "5.1.0",
    "next_bump": "none",
    "reason": "5.1.0 adds the Team Tasks surface to the Node SDK (createTeamTask/appendTeamTaskEvent/updateTeamTask, +3 methods in sdk/dashclaw.js) - a real SDK source change, so the Node SDK IS republished to npm at 5.1.0. A default 30s request timeout (new timeoutMs constructor option, ETIMEDOUT on expiry) also ships in sdk/dashclaw.js and is included in the next Node SDK publish.",
    "domains": [
      "platform",
      "tasks"
    ]
  },
  "python": {
    "current_version": "5.1.0",
    "next_bump": "none",
    "reason": "5.1.0 is platform-only for Python (README + version stamp; Team Tasks methods are Node-first) - no Python SDK source change; the version advances per the unified model but the SDK is NOT republished; PyPI stays at the last SDK release.",
    "domains": [
      "platform"
    ]
  },
  "reason": "5.1.0 ships the Team Tasks surface: dashclaw_task_* MCP tools (+3), Node SDK team task methods (+3), and the /tasks page. Release-plan realigned after v5.1.0 shipped without updating this contract, which held CI red on main for 4 consecutive pushes.",
  "domains": [
    "platform",
    "tasks"
  ]
}
