/supergoal Raise DashClaw's Repowise code-health so every structurally-fixable file scores >=9.5/10 and the hotspot-health average climbs from 5.46 toward >=8.5, by REMOVING structural complexity only. This is a pure behavior-preserving refactor + test-coverage sprint, not a feature change. Run it as an ultracode dynamic workflow: fan out one worktree-isolated agent per file-cluster, each gated on a full re-score, then integrate green diffs sequentially.

MEASUREMENT (the actual success metric — score against this, not vibes):
- After each phase run `repowise update` then `repowise health <file>` and `repowise health` for the dashboard. A phase passes only when every file it touched is >=9.5 OR its remaining deficit is 100% historical biomarkers (change_entropy, prior_defect, churn_risk, co_change_scatter, function_hotspot) with all STRUCTURAL biomarkers (nested_complexity, complex_method, large_method, bumpy_road, dry_violation, complex_conditional, hidden_coupling, untested_hotspot) eliminated.
- Historical biomarkers are git-log-derived and CANNOT be fixed by editing code — do not attempt to game them. For histo

HARD CONSTRAINTS (violating any of these fails the phase):
- NO behavior changes. Every refactor is structure-only. Characterize first: confirm the file's tests are green BEFORE touching it; if a low-score file is untested, WRITE characterization tests first (this also clears its untested_hotspot biomarker), then refactor under green.
- DO NOT change the public API surface of the published SDKs (sdk/dashclaw.js, sdk-python/dashclaw/client.py). Internal-only extraction of private helpers, and only if contracts.sdk-surface.test.js and the SDK parity tests stay green. If a structural fix would alter an exported signature, STOP and leave it.
- middleware.js is the auth core — every authenticated request flows through it; a regression 401s the whole app. Refactor it last, alone, in its own phase, behind a full green vitest run + build. Split the 287-CCN middleware() function into named, tested helpers without changing routing/auth behavior.
- No direct SQL in app/api/**/route.js (route-sql:check blocks it) — use repositories. Don't hand-edit generated artifacts (app/lib/doctor/generated/, public/livingcode/, the platform-intelligence download bundle); regenerate via npm run livingcode:refresh instead.
- Match existing repo style. No new frameworks, no new abstractions beyond what removing the complexity requires, no renames for aesthetics, no opportunistic cleanup of code outside the target file.

VERIFICATION GATE (every phase, read the output before claiming pass):
- npm run lint
- npx vitest run  (the FULL suite — targeted runs miss cross-file regressions)
- npm run build   (webpack build; do NOT use turbopack/npx next build — it throws ~990 false errors here)
- npm run typecheck for any changed .ts file (vitest transpiles without type-checking; tsc in the build will catch what vitest won't)
- The CI contract checks: npm run openapi:check, api:inventory:check, route-sql:check, version:check, and node scripts/check-doc-counts.mjs --strict
- Then the re-score above.

WORKTREE GOTCHA: this repo checks out CRLF in worktrees, which fails ~4 line-ending-sensitive vitest tests that pass on ma
3. app/lib/integrity/verify* (4.0, untested_hotspot) — add tests, then simplify.
4. scripts/install-hooks.mjs (2.1), scripts/setup.mjs (2.4), cli/bin/dashclaw.js (2.1), cli/lib/doctor.js (2.6) — tooling/CLI cluster.
5. app/lib/guard.ts (3.9, CCN 122) — governance core; behavior-preserving extraction under full green guard tests.
6. SDK internals: sdk/dashclaw.js (1.0) + sdk-python/dashclaw/client.py (1.0) — PRIVATE-helper extraction ONLY, public surface frozen, parity tests green. Expect a historical ceiling here.
7. middleware.js (1.9, CCN 287) — LAST, alone, auth-critical. Then next.config.js if any structural lever remains.
After all phases: run repowise health once more and report the before/after average + per-file deltas, and an explicit list of files still below 9.5 with their residual-historical justification.

STOP CONDITIONS: stop and ask if any structural fix forces a public SDK signature change, an auth/routing behavior change in middleware, a schema/migration, or a new dependency. Do not defer in-scope structural findings — fix them in the owning phase. One confirmation before execution, then run to completion without babysitting.